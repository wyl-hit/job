/* 
 * Author: Damien Brian
 * Load script file and append to the head of the body.
 * Use it for developpement purpose, and upload a minified
 * javascript file for production 
 */


var LoaderJS = {
  /*
   * Set here all the javascript file to load
   */
  "scriptToLoad": ["js/Test1.js", "js/Test2.js"],

  /*
   * Load all script file.
   */
  loadScript : function()
  {
	  alert("Load");
	  LoaderJS.scriptToLoad.forEach(function(url, index, array){
  		var script = document.createElement('script');
  		script.setAttribute("src", url);
  		document.getElementsByTagName("head").item(0).appendChild(script);
  	});
  },
  

};
/* Polyfill for IE. */
function addListener(obj, eventName, listener) {
    if (obj.addEventListener) {
        obj.addEventListener(eventName, listener, false);
    } else {
        obj.attachEvent("on" + eventName, listener);
    }
}
addListener(document, "DOMContentLoaded", LoaderJS.loadScript); 